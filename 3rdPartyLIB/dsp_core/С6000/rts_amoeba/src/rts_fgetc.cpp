// ---------------------------------------------------------------------------
// int fgetc( FILE* stream );
// ---------------------------------------------------------------------------
#include <rts.h>
// ---------------------------------------------------------------------------
int fgetc( FILE* stream )
{
  uint8 c;
  if( fread( &c, 1, 1, stream ) != 1 ) return EOF;
  return (int)c;
}
// ---------------------------------------------------------------------------
