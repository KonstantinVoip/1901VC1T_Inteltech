// ---------------------------------------------------------------------------
// extern void free( void *packet )
// ---------------------------------------------------------------------------
#include <os.h>
// ---------------------------------------------------------------------------
void free( void *packet )
{
  heap_free( prc_heap(), packet );
}
// ---------------------------------------------------------------------------
