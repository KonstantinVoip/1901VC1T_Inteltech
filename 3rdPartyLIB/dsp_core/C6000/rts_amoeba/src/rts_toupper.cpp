// ---------------------------------------------------------------------------
// int toupper( int c ) // cp1251 version
// ---------------------------------------------------------------------------
int toupper( int c )
{
  if( ( ( c >= 97 ) && ( c <= 122 ) ) || ( ( c >= 224 ) && ( c <= 255 ) ) ) return c - 32;
  if( c == 184 ) return 168;
  return c;
}
// ---------------------------------------------------------------------------
