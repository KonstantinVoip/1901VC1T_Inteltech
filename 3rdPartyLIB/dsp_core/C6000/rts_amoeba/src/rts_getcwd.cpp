// ---------------------------------------------------------------------------
// char* getcwd( char* buf, size_t size )
// ---------------------------------------------------------------------------
#include <rts.h>
// ---------------------------------------------------------------------------
char* getcwd( char* buf, size_t size )
{
// -- IT MUST BE DESTROYED -[ OLD FS TRIGGER ]--------------------------------
//  return old_getcwd( buf, size ); // -----------------------------------------
// -- IT MUST BE DESTROYED -[ OLD FS TRIGGER ]--------------------------------

  return NULL;
}
// ---------------------------------------------------------------------------
